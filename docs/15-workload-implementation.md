# 15 - 工作负载资源实施计划

> 工作负载资源创建的详细实施步骤

---

## 1. 实施总览

### 1.1 模块划分

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         工作负载资源实施路线图                               │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  网络资源 (高优先级)                                                        │
│  ══════════════════                                                         │
│  预计: 0.5 天                                                               │
│  ┌─────────────────────────────────────────────────────────────────┐       │
│  │ • 创建 Training Jobs 安全组                                      │       │
│  │ • 创建 Processing Jobs 安全组                                    │       │
│  │ • 创建 Inference Endpoints 安全组                                │       │
│  │ • VPC 脚本一次性创建所有资源                                     │       │
│  └─────────────────────────────────────────────────────────────────┘       │
│                              │                                              │
│                              ▼                                              │
│  容器镜像 (中优先级，可选)                                                  │
│  ════════════════════════                                                   │
│  预计: 0.5 天                                                               │
│  ┌─────────────────────────────────────────────────────────────────┐       │
│  │ • 创建 ECR 仓库结构                                              │       │
│  │ • 配置 Lifecycle Policy                                          │       │
│  │ • 推送基础镜像 (可选)                                            │       │
│  └─────────────────────────────────────────────────────────────────┘       │
│                              │                                              │
│                              ▼                                              │
│  模型治理 (高优先级)                                                        │
│  ══════════════════                                                         │
│  预计: 0.5 天                                                               │
│  ┌─────────────────────────────────────────────────────────────────┐       │
│  │ • 创建 Model Package Groups                                      │       │
│  │ • 配置 IAM 权限                                                  │       │
│  │ • 测试模型注册流程                                               │       │
│  └─────────────────────────────────────────────────────────────────┘       │
│                              │                                              │
│                              ▼                                              │
│  日志与监控 (低优先级)                                                      │
│  ════════════════════                                                       │
│  预计: 0.5 天                                                               │
│  ┌─────────────────────────────────────────────────────────────────┐       │
│  │ • 配置 CloudWatch 日志保留策略                                   │       │
│  │ • 创建关键指标告警                                               │       │
│  └─────────────────────────────────────────────────────────────────┘       │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 1.2 优先级说明

| 模块 | 优先级 | 状态 | 原因 | 依赖 |
|------|--------|:----:|------|------|
| **网络资源** | 🔴 高 | ✅ 已实现 | Processing/Training 必需 | 基础设施完成 |
| **容器镜像** | 🟡 中 | ✅ 已实现 | 仅自定义镜像需要 | 网络资源 |
| **模型治理** | 🔴 高 | ✅ 已实现 | 模型版本管理必需 | 基础设施完成 |
| **日志监控** | 🟢 低 | 📋 待实现 | 可后续配置 | 网络资源 |

---

## 2. 网络资源（VPC 安全组）

### 2.1 脚本结构 ✅

```
scripts/02-vpc/
├── 00-init.sh                          # 初始化
├── 01-create-security-groups.sh        # 创建核心安全组 (Studio + VPC Endpoints)
├── 02-create-vpc-endpoints.sh          # 创建 VPC Endpoints
├── 03-create-workload-sgs.sh           # ✅ 创建工作负载安全组
├── setup-all.sh                        # ✅ 一次性创建所有资源
├── verify.sh                           # ✅ 验证所有资源
└── cleanup.sh                          # 清理资源
```

### 2.2 执行方式

```bash
# 一次性创建所有 VPC 资源（安全组 + Endpoints + 工作负载安全组）
cd scripts/02-vpc
./setup-all.sh

# 验证配置
./verify.sh
```

### 2.3 创建的安全组

| 安全组名称 | 类型 | 用途 |
|-----------|------|------|
| `{TAG_PREFIX}-studio` | 核心 | SageMaker Studio 实例 |
| `{TAG_PREFIX}-vpc-endpoints` | 核心 | VPC Endpoints |
| `{TAG_PREFIX}-training` | 工作负载 | Training Jobs (分布式训练) |
| `{TAG_PREFIX}-processing` | 工作负载 | Processing Jobs (Spark 集群) |
| `{TAG_PREFIX}-inference` | 工作负载 | Inference Endpoints (推理服务) |

### 2.4 安全组规则详情

#### Training SG

```
入站:
- All Traffic from self (分布式训练)
- HTTPS (443) from VPC CIDR

出站:
- HTTPS (443) to 0.0.0.0/0 (AWS 服务)
- All Traffic to self (分布式训练)
```

#### Processing SG

```
入站:
- All Traffic from self (Spark 集群)
- HTTPS (443) from VPC CIDR

出站:
- HTTPS (443) to 0.0.0.0/0 (AWS 服务)
- All Traffic to self (Spark 集群)
```

#### Inference SG

```
入站:
- HTTPS (443) from VPC CIDR (推理请求)
- TCP 8080 from VPC CIDR (容器端口)

出站:
- HTTPS (443) to 0.0.0.0/0 (AWS 服务)
```

### 2.5 输出文件

```
scripts/02-vpc/output/security-groups.env

# 内容:
SG_SAGEMAKER_STUDIO={sg-id}
SG_VPC_ENDPOINTS={sg-id}
SG_TRAINING={sg-id}
SG_PROCESSING={sg-id}
SG_INFERENCE={sg-id}
```

### 2.6 验收标准

- [ ] 五个安全组创建成功（2 核心 + 3 工作负载）
- [ ] 安全组规则符合设计
- [ ] 输出文件包含所有 SG ID
- [ ] verify.sh 检查通过

---

## 3. 容器镜像（ECR）✅

### 3.1 脚本结构

```
scripts/06-ecr/                         # ✅ 已实现
├── 00-init.sh
├── 01-create-repositories.sh           # 含 Lifecycle Policy 配置
├── setup-all.sh
├── verify.sh
├── cleanup.sh
└── README.md
```

### 3.2 创建的仓库

```
根据 .env.shared 配置自动创建:

共享仓库:
- {COMPANY}-sagemaker-shared/base-sklearn
- {COMPANY}-sagemaker-shared/base-pytorch
- {COMPANY}-sagemaker-shared/base-xgboost

项目仓库 (按需):
- {COMPANY}-sm-{team}-{project}/preprocessing
- {COMPANY}-sm-{team}-{project}/training
- {COMPANY}-sm-{team}-{project}/inference
```

### 3.3 环境变量

```bash
# .env.shared 新增
ENABLE_ECR=true                  # 是否启用 ECR 模块
ECR_SHARED_REPOS="base-sklearn base-pytorch base-xgboost"
ECR_PROJECT_REPOS="preprocessing training inference"
ECR_IMAGE_RETENTION=10           # 保留最近 10 个镜像
```

### 3.4 验收标准

- [ ] 共享仓库创建成功
- [ ] 项目仓库创建成功（如启用）
- [ ] Lifecycle Policy 配置正确
- [ ] 测试 docker push 成功

---

## 4. 模型治理（Model Registry）✅

### 4.1 脚本结构

```
scripts/07-model-registry/              # ✅ 已实现
├── 00-init.sh
├── 01-create-model-groups.sh
├── setup-all.sh
├── verify.sh
├── cleanup.sh
└── README.md
```

### 4.2 创建的资源

```
根据 .env.shared 中的项目配置自动创建:

Model Package Groups:
- {team}-{project}

示例:
- rc-fraud-detection
- rc-anti-money-laundering
- algo-recommendation-engine
```

### 4.3 IAM 权限更新 ✅

Execution Role 已包含 Model Registry 权限（在 `execution-role.json.tpl` 中）:

```json
{
  "Sid": "AllowModelRegistry",
  "Effect": "Allow",
  "Action": [
    "sagemaker:CreateModelPackage",
    "sagemaker:CreateModelPackageGroup",
    "sagemaker:DescribeModelPackage",
    "sagemaker:DescribeModelPackageGroup",
    "sagemaker:ListModelPackages",
    "sagemaker:ListModelPackageGroups",
    "sagemaker:UpdateModelPackage",
    "sagemaker:DeleteModelPackage"
  ],
  "Resource": [
    "arn:aws:sagemaker:${REGION}:${ACCOUNT}:model-package-group/${TEAM}-${PROJECT}",
    "arn:aws:sagemaker:${REGION}:${ACCOUNT}:model-package/${TEAM}-${PROJECT}/*"
  ]
}
```

### 4.4 验收标准

- [ ] Model Package Groups 创建成功
- [ ] 每个项目对应一个 Group
- [ ] 测试模型注册成功
- [ ] IAM 权限验证通过

---

## 5. 日志与监控

### 5.1 配置内容

```
CloudWatch 日志保留策略:
- /aws/sagemaker/ProcessingJobs: 30 天
- /aws/sagemaker/TrainingJobs: 90 天
- /aws/sagemaker/Endpoints: 14 天

CloudWatch 告警 (可选):
- Training Job 失败告警
- Endpoint 错误率告警
- Endpoint 延迟告警
```

### 5.2 脚本结构

```
scripts/09-monitoring/                  # 新增模块
├── 00-init.sh
├── 01-configure-log-retention.sh
├── 02-create-alarms.sh                 # 可选
├── setup-all.sh
└── README.md
```

### 5.3 验收标准

- [ ] 日志保留策略配置成功
- [ ] 告警创建成功（如启用）
- [ ] 测试告警触发正常

---

## 6. 实施时间表

### 6.1 建议顺序

```
Day 1 (上午):
├── 创建 VPC 网络资源（安全组 + Endpoints）
└── 验证 Processing/Training 网络连通

Day 1 (下午):
├── 创建 Model Package Groups
└── 测试模型注册流程

Day 2 (按需):
├── ECR 仓库 (如需自定义镜像)
└── 日志与监控配置
```

### 6.2 依赖关系

```
基础设施 (已完成)
    │
    ├──────────────────────┬─────────────────────┐
    │                      │                     │
    ▼                      ▼                     ▼
网络资源              模型治理               ECR 仓库 (可选)
(VPC 安全组)        (Model Registry)         (容器镜像)
    │                                           │
    │                                           │
    ▼                                           │
日志监控 ◀──────────────────────────────────────┘
```

---

## 7. 回滚方案

### 7.1 VPC 安全组回滚

```bash
# 删除所有 VPC 资源（安全组 + Endpoints）
cd scripts/02-vpc
./cleanup.sh
```

### 7.2 ECR 回滚

```bash
# 删除 ECR 仓库 (会删除所有镜像!)
cd scripts/06-ecr
./cleanup.sh
```

### 7.3 Model Registry 回滚

```bash
# 删除 Model Package Groups (会删除所有版本!)
cd scripts/07-model-registry
./cleanup.sh
```

---

## 8. 检查清单

### 8.1 实施前

- [ ] 基础设施全部完成并验证通过
- [ ] 确认 VPC/Subnet/VPC Endpoints 正常工作
- [ ] 确认 Execution Role 权限正确
- [ ] 备份现有配置

### 8.2 实施中

- [ ] 按顺序执行各模块脚本
- [ ] 每模块完成后运行 verify.sh
- [ ] 记录创建的资源 ID

### 8.3 实施后

- [ ] 运行 Processing Job 测试
- [ ] 运行 Training Job 测试
- [ ] 测试模型注册流程
- [ ] 更新用户使用文档

---

## 9. 风险与缓解

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| 安全组规则过严 | Job 无法启动 | 先放开再收紧，渐进调整 |
| ECR 权限不足 | 无法推送镜像 | 检查 IAM 权限 |
| Model Group 命名冲突 | 创建失败 | 检查现有资源 |
| 跨 AZ 网络问题 | 分布式训练失败 | 确保子网覆盖多 AZ |

---

## 10. 下一步

完成工作负载资源后：

1. 用户可以开始 Processing/Training 工作负载
2. 参考快速入门文档进行开发
3. 按需配置 ECR 和自定义镜像
4. 建立模型审批流程

相关文档：
- [10 - Processing 快速入门](10-sagemaker-processing.md)
- [12 - Training 快速入门](12-sagemaker-training.md)
- [13 - Inference 快速入门](13-realtime-inference.md)
