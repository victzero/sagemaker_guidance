# =============================================================================
# .env.local.example - SageMaker Domain 模块特有配置
# =============================================================================
# 使用方法:
#   1. 复制此文件: cp .env.local.example .env.local
#   2. 编辑 .env.local 填入实际值
#   3. 共享配置（COMPANY, AWS_ACCOUNT_ID 等）请配置在 ../env.shared
# =============================================================================

# === Domain 配置 ===
# Domain 名称（可选，默认: ${COMPANY}-ml-platform）
# DOMAIN_NAME="acme-ml-platform"

# === 安全配置 ===
# 是否禁用文件下载 (true/false)
# true: 部署 Lifecycle Config 隐藏下载按钮 (推荐生产环境)
# false: 允许文件下载 (仅限开发环境)
DISABLE_STUDIO_DOWNLOAD=true

# === Lifecycle Config (自动关机) ===
# 空闲超时时间（分钟），超时后自动关闭实例
IDLE_TIMEOUT_MINUTES=60

# Lifecycle Config 名称前缀 (脚本自动追加后缀)
# LIFECYCLE_CONFIG_NAME="auto-shutdown"

# === 默认实例配置 ===
# 默认实例类型
DEFAULT_INSTANCE_TYPE="ml.t3.medium"

# 默认 EBS 存储大小 (GB)
DEFAULT_EBS_SIZE_GB=100

# =============================================================================
# 以下配置已移至 ../.env.shared:
#   - COMPANY, AWS_ACCOUNT_ID, AWS_REGION
#   - VPC_ID, VPC_CIDR
#   - PRIVATE_SUBNET_1_ID, PRIVATE_SUBNET_2_ID
#   - TEAMS, TEAM_*_FULLNAME
#   - *_PROJECTS, *_USERS
# =============================================================================
