# =============================================================================
# SageMaker IAM Setup - Local Environment Variables
# =============================================================================
# IAM 模块特有的配置（会覆盖 .env.shared 中的同名变量）
#
# 使用方法:
#   1. 复制此文件: cp .env.local.example .env.local
#   2. 编辑 .env.local 填入实际值（通常使用默认值即可）
# =============================================================================

# -----------------------------------------------------------------------------
# IAM 资源路径
# -----------------------------------------------------------------------------
# 所有 IAM 资源使用此路径，便于筛选和管理
# 格式: /${COMPANY}-sagemaker/
# 示例: /acme-sagemaker/
#
# 筛选命令:
#   aws iam list-users --path-prefix /acme-sagemaker/
#   aws iam list-groups --path-prefix /acme-sagemaker/
#   aws iam list-roles --path-prefix /acme-sagemaker/
#   aws iam list-policies --scope Local --path-prefix /acme-sagemaker/
# -----------------------------------------------------------------------------
# 注意: 如果 .env.shared 中设置了 COMPANY=acme，则此处可留空
# 脚本会自动设置为 /${COMPANY}-sagemaker/
# IAM_PATH=/${COMPANY}-sagemaker/

# -----------------------------------------------------------------------------
# 密码配置
# -----------------------------------------------------------------------------
# 初始密码格式: {PREFIX}{username}{SUFFIX}
# 用户首次登录必须重置密码
#
# 示例: Welcome#alice@2024
PASSWORD_PREFIX="Welcome#"
PASSWORD_SUFFIX="@2024"

# =============================================================================
# 注意事项
# =============================================================================
#
# 1. 此文件为可选配置，大多数情况下使用默认值即可
# 2. 基础配置（COMPANY, TEAMS, USERS 等）请在 ../.env.shared 中设置
# 3. 此文件中的变量会覆盖 .env.shared 中的同名变量
#
# =============================================================================
