# =============================================================================
# SageMaker IAM 配置 - 环境变量模板
# =============================================================================
# 使用说明:
#   1. 复制此文件: cp .env.example .env
#   2. 编辑 .env 文件，填入实际值
#   3. 运行脚本前会自动加载 .env
# =============================================================================

# -----------------------------------------------------------------------------
# 基础配置
# -----------------------------------------------------------------------------

# 公司/组织名称前缀 (用于资源命名，建议小写字母)
COMPANY=acme

# AWS 账号 ID (12位数字)
AWS_ACCOUNT_ID=123456789012

# AWS 区域
AWS_REGION=ap-southeast-1

# -----------------------------------------------------------------------------
# 团队配置
# -----------------------------------------------------------------------------

# 团队列表 (空格分隔)
TEAMS="rc algo"

# 团队全称映射
TEAM_RC_FULLNAME="risk-control"
TEAM_ALGO_FULLNAME="algorithm"

# -----------------------------------------------------------------------------
# 项目配置
# -----------------------------------------------------------------------------

# 风控团队项目列表 (空格分隔)
RC_PROJECTS="project-a project-b"

# 算法团队项目列表 (空格分隔)
ALGO_PROJECTS="project-x project-y"

# -----------------------------------------------------------------------------
# 用户配置
# -----------------------------------------------------------------------------

# 风控团队 Project-A 用户列表 (空格分隔)
RC_PROJECT_A_USERS="alice bob carol"

# 风控团队 Project-B 用户列表 (空格分隔)
RC_PROJECT_B_USERS="david emma"

# 算法团队 Project-X 用户列表 (空格分隔)
ALGO_PROJECT_X_USERS="frank grace henry"

# 算法团队 Project-Y 用户列表 (空格分隔)
ALGO_PROJECT_Y_USERS="ivy jack"

# 管理员用户列表 (空格分隔)
ADMIN_USERS="jason"

# -----------------------------------------------------------------------------
# 密码策略 (首次登录需重置密码)
# -----------------------------------------------------------------------------

# 初始密码前缀 (实际密码为: 前缀 + 用户名 + 后缀)
# 例如: Temp@alice!2024
PASSWORD_PREFIX="Temp@"
PASSWORD_SUFFIX="!2024"

# -----------------------------------------------------------------------------
# 可选配置
# -----------------------------------------------------------------------------

# 是否启用 MFA 强制 (true/false)
REQUIRE_MFA=true

# IAM 路径前缀 (用于组织 IAM 资源)
IAM_PATH="/sagemaker/"

# 输出 JSON 文件目录
OUTPUT_DIR="./output"

# 是否启用 dry-run 模式 (只打印命令，不执行)
DRY_RUN=false
