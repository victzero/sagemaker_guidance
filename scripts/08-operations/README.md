# SageMaker 运维操作脚本

用于管理已部署 SageMaker 环境的增量变更操作。

---

## 📋 目录

- [场景总览](#场景总览)
- [脚本清单](#脚本清单)
- [使用指南](#使用指南)
- [实施计划](#实施计划)

---

## 场景总览

基于当前架构（一用户多 Profile、4 角色分离），支持以下 **8 类核心场景**：

| 类别         | 场景                      | 脚本                               | 优先级 | 状态 |
| ------------ | ------------------------- | ---------------------------------- | ------ | ---- |
| **用户管理** | 1. 新增用户到已有项目     | `user/add-user.sh`                 | P0     | ✅   |
|              | 2. 将已有用户添加到新项目 | `user/add-user-to-project.sh`      | P0     | ✅   |
|              | 3. 从项目移除用户         | `user/remove-user-from-project.sh` | P1     | ✅   |
|              | 4. 完全删除用户           | `user/delete-user.sh`              | P2     | ✅   |
| **项目管理** | 5. 新增项目               | `project/add-project.sh`           | P1     | ✅   |
|              | 6. 删除项目               | `project/delete-project.sh`        | P3     | ✅   |
| **团队管理** | 7. 新增团队               | `team/add-team.sh`                 | P3     | ✅   |
|              | 8. 删除团队               | `team/delete-team.sh`              | P3     | ✅   |
| **查询**     | 列出用户                  | `query/list-users.sh`              | P2     | ✅   |
|              | 列出项目                  | `query/list-projects.sh`           | P2     | ✅   |

---

## 场景详细说明

### 场景 1：新增用户到已有项目 ⭐ P0

**触发条件**：新员工入职，需要加入现有项目

**涉及资源变更**：

| 资源类型             | 操作     | 命名格式                          | 说明                   |
| -------------------- | -------- | --------------------------------- | ---------------------- |
| IAM User             | 创建     | `sm-{team}-{user}`                | 新建用户               |
| IAM Group            | 添加成员 | `sagemaker-{team}-{project}`      | 加入项目组             |
| IAM Group            | 添加成员 | `sagemaker-{team-fullname}`       | 加入团队组             |
| User Profile         | 创建     | `profile-{team}-{project}-{user}` | SageMaker Profile      |
| Private Space        | 创建     | `space-{team}-{project}-{user}`   | 私有工作空间           |
| Console Password     | 设置     | -                                 | 可选，首次登录强制修改 |
| Permissions Boundary | 绑定     | `SageMaker-User-Boundary`         | 权限边界               |

**依赖关系**：

```
IAM User → 加入 Groups → User Profile → Private Space
```

**使用方法**：

```bash
./user/add-user.sh

# 交互式引导：
# > 选择团队: [1] rc (risk-control)
# > 选择项目: [1] fraud-detection
# > 输入用户名 (仅字母数字): alice
# > 启用 Console 登录? [y/N]: y
#
# 显示资源清单，确认后执行
```

---

### 场景 2：将已有用户添加到新项目 ⭐ P0

**触发条件**：员工跨项目协作，需要访问另一个项目

**涉及资源变更**：

| 资源类型      | 操作     | 说明                     |
| ------------- | -------- | ------------------------ |
| IAM User      | 无变更   | 已存在                   |
| IAM Group     | 添加成员 | 将用户加入新项目的 Group |
| User Profile  | 创建     | 新项目下的 Profile       |
| Private Space | 创建     | 新项目下的 Space         |

**注意**：用户登录后会看到多个 Profile，可切换项目上下文

**使用方法**：

```bash
./user/add-user-to-project.sh

# 交互式引导：
# > 输入 IAM 用户名 (如 sm-rc-alice): sm-rc-alice
# > 选择要加入的项目: [1] fraud-detection [2] anti-money-laundering
#
# 显示资源清单，确认后执行
```

---

### 场景 3：从项目移除用户 (P1)

**触发条件**：员工退出项目，但仍在其他项目工作

**涉及资源变更**：

| 资源类型      | 操作     | 说明                     |
| ------------- | -------- | ------------------------ |
| Private Space | 删除     | 先停止 App，再删除 Space |
| User Profile  | 删除     | 删除该项目的 Profile     |
| IAM Group     | 移除成员 | 从项目 Group 移除        |

**数据处理**：

- Space 中的数据需提前备份（EBS 卷会丢失）
- S3 项目数据不受影响（按项目而非用户）

---

### 场景 4：完全删除用户 (P2)

**触发条件**：员工离职

**涉及资源变更**（按顺序）：

1. 所有 Private Space（每个项目一个）
2. 所有 User Profile（每个项目一个）
3. 从所有 IAM Group 移除
4. IAM User（含 Access Key、MFA 设备、LoginProfile）

**安全要求**：

- 立即撤销所有访问权限
- 审计日志记录
- 数据归档（如需要）

---

### 场景 5：新增项目 (P1)

**触发条件**：团队启动新的 ML 项目

**涉及资源变更**：

| 资源类型        | 数量 | 命名格式                                   |
| --------------- | ---- | ------------------------------------------ |
| IAM Group       | 1    | `sagemaker-{team}-{project}`               |
| IAM Roles       | 4    | `SageMaker-{Team}-{Project}-{Role}`        |
| IAM Policies    | 12   | 项目级策略                                 |
| S3 Bucket       | 1    | `{prefix}-sm-{team}-{project}`             |
| Security Groups | 3    | `sg-sm-{team}-{project}-{workload}` (可选) |

**用户添加**：项目创建后，再按场景 1/2 添加用户

---

### 场景 6：删除项目 (P3)

**触发条件**：项目结束或合并

**涉及资源删除顺序**：

1. 所有 Private Space
2. 所有 User Profile
3. IAM Group
4. IAM Roles (4 个)
5. IAM Policies (12 个)
6. Security Groups (3 个)
7. S3 Bucket (建议保留/归档)

**高风险操作**：需输入项目名确认

---

### 场景 7：新增团队 (P3)

**触发条件**：组织扩展，新部门需要独立环境

本质是创建一个新的"命名空间"：

1. 创建团队级 IAM 资源
2. 按场景 5 创建项目
3. 按场景 1 添加用户

---

### 场景 8：删除团队 (P3)

**触发条件**：部门重组或撤销

需先删除所有项目（场景 6），再删除团队级资源

---

## 脚本清单

```
scripts/08-operations/
├── README.md                         # 本文档
├── 00-init.sh                        # 初始化脚本
│
├── user/                             # 用户管理
│   ├── add-user.sh                   # 场景1: 新增用户到项目 [P0] ✅
│   ├── add-user-to-project.sh        # 场景2: 已有用户加入新项目 [P0] ✅
│   ├── remove-user-from-project.sh   # 场景3: 从项目移除用户 [P1] ✅
│   └── delete-user.sh                # 场景4: 完全删除用户 [P2] ✅
│
├── project/                          # 项目管理
│   ├── add-project.sh                # 场景5: 新增项目 [P1] ✅
│   └── delete-project.sh             # 场景6: 删除项目 [P3] ✅
│
├── team/                             # 团队管理
│   ├── add-team.sh                   # 场景7: 新增团队 [P3] ✅
│   └── delete-team.sh                # 场景8: 删除团队 [P3] ✅
│
└── query/                            # 查询工具
    ├── list-users.sh                 # 列出用户 [P2] ✅
    └── list-projects.sh              # 列出项目 [P2] ✅
```

---

## 使用指南

### 前置条件

1. 已完成初始部署（01-iam ~ 07-model-registry）
2. 已配置 `.env.shared` 文件
3. AWS CLI 已配置并有足够权限

### 配置加载

此模块会自动加载以下配置文件（按顺序，后加载的覆盖先加载的）：

```
加载顺序:
1. scripts/.env.shared                     # 共享配置（必须）
2. scripts/01-iam/.env.local               # IAM 配置 (PASSWORD_*, IAM_PATH)
3. scripts/04-sagemaker-domain/.env.local  # Domain 配置 (DOMAIN_ID)
4. scripts/05-user-profiles/.env.local     # Profile 配置 (SPACE_EBS_SIZE_GB)
5. scripts/08-operations/.env.local        # 本地覆盖（可选）
```

**配置来源说明**：

| 配置变量            | 来源                             | 用途                   |
| ------------------- | -------------------------------- | ---------------------- |
| `COMPANY`           | `.env.shared`                    | 公司前缀               |
| `TEAMS`             | `.env.shared`                    | 团队列表               |
| `*_PROJECTS`        | `.env.shared`                    | 项目列表               |
| `*_USERS`           | `.env.shared`                    | 用户列表               |
| `IAM_PATH`          | `01-iam/.env.local`              | IAM 资源路径           |
| `PASSWORD_PREFIX`   | `01-iam/.env.local`              | 初始密码前缀           |
| `PASSWORD_SUFFIX`   | `01-iam/.env.local`              | 初始密码后缀           |
| `DOMAIN_ID`         | `04-sagemaker-domain/.env.local` | SageMaker Domain ID    |
| `SPACE_EBS_SIZE_GB` | `05-user-profiles/.env.local`    | Private Space EBS 大小 |

> **注意**：通常不需要创建 `08-operations/.env.local`，除非需要覆盖其他模块的配置。

### 交互流程

所有脚本遵循统一的交互模式：

```
┌─────────────────────────────────────────┐
│  1. 交互式输入（选择团队/项目/用户）     │
├─────────────────────────────────────────┤
│  2. 显示资源变更清单                     │
│     - 新增资源列表                       │
│     - 变更资源列表                       │
├─────────────────────────────────────────┤
│  3. 用户确认 [y/N]                       │
├─────────────────────────────────────────┤
│  4. 执行操作                             │
├─────────────────────────────────────────┤
│  5. 显示执行结果                         │
└─────────────────────────────────────────┘
```

### 命名规范

| 资源             | 命名格式                                   | 示例                                                 |
| ---------------- | ------------------------------------------ | ---------------------------------------------------- |
| IAM User         | `sm-{team}-{user}`                         | `sm-rc-alice`                                        |
| IAM Group (项目) | `sagemaker-{team}-{project}`               | `sagemaker-rc-fraud-detection`                       |
| IAM Group (团队) | `sagemaker-{team-fullname}`                | `sagemaker-risk-control`                             |
| User Profile     | `profile-{team}-{project_short}-{user}`    | `profile-rc-fraud-alice`                             |
| Private Space    | `space-{team}-{project_short}-{user}`      | `space-rc-fraud-alice`                               |
| Execution Role   | `SageMaker-{Team}-{Project}-ExecutionRole` | `SageMaker-RiskControl-FraudDetection-ExecutionRole` |

---

## 实施计划

### Phase 1: P0 核心操作 (立即实现)

| 任务               | 脚本                          | 状态 |
| ------------------ | ----------------------------- | ---- |
| 新增用户到项目     | `user/add-user.sh`            | ✅   |
| 已有用户加入新项目 | `user/add-user-to-project.sh` | ✅   |

### Phase 2: P1 常用操作 (✅ 已完成)

| 任务           | 脚本                               | 状态 |
| -------------- | ---------------------------------- | ---- |
| 从项目移除用户 | `user/remove-user-from-project.sh` | ✅   |
| 新增项目       | `project/add-project.sh`           | ✅   |

### Phase 3: P2 查询与删除 (✅ 已完成)

| 任务         | 脚本                     | 状态 |
| ------------ | ------------------------ | ---- |
| 完全删除用户 | `user/delete-user.sh`    | ✅   |
| 列出用户     | `query/list-users.sh`    | ✅   |
| 列出项目     | `query/list-projects.sh` | ✅   |

### Phase 4: P3 低频操作 (✅ 已完成)

| 任务     | 脚本                        | 状态 |
| -------- | --------------------------- | ---- |
| 删除项目 | `project/delete-project.sh` | ✅   |
| 新增团队 | `team/add-team.sh`          | ✅   |
| 删除团队 | `team/delete-team.sh`       | ✅   |

---

## 安全设计

### 操作确认

- **所有操作**：执行前列出完整资源清单
- **确认方式**：用户输入 `y` 确认
- **删除操作**：需输入资源名称二次确认

### 审计日志

所有操作会记录到 CloudTrail，可通过以下方式查询：

```bash
# 查看最近的 IAM 操作
aws cloudtrail lookup-events \
    --lookup-attributes AttributeKey=EventSource,AttributeValue=iam.amazonaws.com \
    --start-time $(date -d '1 hour ago' --utc +%Y-%m-%dT%H:%M:%SZ)
```

### 权限要求

执行脚本的 IAM 用户/角色需要以下权限：

- `iam:CreateUser`, `iam:AddUserToGroup`, `iam:CreateLoginProfile`
- `sagemaker:CreateUserProfile`, `sagemaker:CreateSpace`
- `sagemaker:DescribeUserProfile`, `sagemaker:DescribeSpace`

---

## 错误处理

| 错误类型       | 处理方式             |
| -------------- | -------------------- |
| 用户已存在     | 提示并继续（幂等）   |
| Profile 已存在 | 提示并继续（幂等）   |
| 权限不足       | 显示错误信息，退出   |
| 网络错误       | 显示错误信息，可重试 |

---

## 常见问题

### Q: 新用户如何登录 SageMaker Studio?

1. 使用初始密码登录 AWS Console（首次需修改密码）
2. 绑定 MFA 设备
3. 重新登录后访问 SageMaker Studio
4. 选择对应项目的 User Profile

### Q: 用户可以同时访问多个项目吗?

可以。每个项目会创建独立的 User Profile，用户登录后切换 Profile 即可。

### Q: 删除 Space 会丢失数据吗?

会。Space 的 EBS 卷会被删除。建议操作前：

1. 将重要数据保存到 S3
2. 导出 Notebook 文件

---

## 版本记录

| 版本 | 日期       | 说明                                |
| ---- | ---------- | ----------------------------------- |
| v1.0 | 2025-01-06 | 初始版本，实现 P0 脚本              |
| v1.1 | 2025-01-06 | 实现 P1 脚本，简化配置加载          |
| v1.2 | 2025-01-06 | 实现 P2 脚本 (delete-user, query)   |
| v1.3 | 2025-01-06 | 实现 P3 脚本 (team/project 管理) 🎉 |
