# =============================================================================
# .env.local.example - User Profiles 模块配置
# =============================================================================
# 此模块主要使用 ../.env.shared 中的共享配置
# 包括: TEAMS, TEAM_*_FULLNAME, *_PROJECTS, *_USERS
# =============================================================================

# 通常不需要额外配置
# 如需覆盖某些设置，可在此添加
