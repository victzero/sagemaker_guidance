# =============================================================================
# SageMaker VPC 配置 - 环境变量模板
# =============================================================================
# 使用说明:
#   1. 复制此文件: cp .env.example .env
#   2. 编辑 .env 文件，填入实际值
#   3. 运行脚本前会自动加载 .env
# =============================================================================

# -----------------------------------------------------------------------------
# 基础配置
# -----------------------------------------------------------------------------

# 公司/组织名称前缀
COMPANY=acme

# AWS 账号 ID (12位数字)
AWS_ACCOUNT_ID=123456789012

# AWS 区域
AWS_REGION=ap-northeast-1

# -----------------------------------------------------------------------------
# VPC 配置 (使用现有 VPC)
# -----------------------------------------------------------------------------

# 现有 VPC ID
VPC_ID=vpc-0abc123def456

# VPC CIDR 范围
VPC_CIDR=10.0.0.0/16

# -----------------------------------------------------------------------------
# 子网配置 (使用现有 Private Subnets)
# -----------------------------------------------------------------------------

# Private Subnet 1 (可用区 A)
PRIVATE_SUBNET_1_ID=subnet-0abc123def456
PRIVATE_SUBNET_1_AZ=ap-southeast-1a

# Private Subnet 2 (可用区 B)
PRIVATE_SUBNET_2_ID=subnet-0def456abc789
PRIVATE_SUBNET_2_AZ=ap-southeast-1b

# -----------------------------------------------------------------------------
# VPC Endpoints 配置
# -----------------------------------------------------------------------------

# 是否创建可选的 Endpoints (true/false)
CREATE_ECR_ENDPOINTS=true
CREATE_KMS_ENDPOINT=false
CREATE_SSM_ENDPOINT=false

# -----------------------------------------------------------------------------
# 路由表配置
# -----------------------------------------------------------------------------

# Private Subnet 1 路由表 ID (用于 S3 Gateway Endpoint)
ROUTE_TABLE_1_ID=rtb-0abc123def456

# Private Subnet 2 路由表 ID
ROUTE_TABLE_2_ID=rtb-0def456abc789

# -----------------------------------------------------------------------------
# 可选配置
# -----------------------------------------------------------------------------

# 输出 JSON 文件目录
OUTPUT_DIR="./output"

# 是否启用 dry-run 模式 (只打印命令，不执行)
DRY_RUN=false

# 资源标签前缀
TAG_PREFIX="sagemaker"
