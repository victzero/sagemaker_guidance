# =============================================================================
# 02-vpc - VPC 模块特有配置
# =============================================================================
# 共享配置请编辑: ../scripts/.env.shared
# 此文件包含 VPC 模块特有的配置项（必填）
# =============================================================================

# -----------------------------------------------------------------------------
# VPC 配置（必填）
# -----------------------------------------------------------------------------

# VPC ID（从 AWS 控制台获取）
VPC_ID=vpc-xxxxxxxxxxxxxxxxx

# VPC CIDR（例如 10.0.0.0/16）
VPC_CIDR=10.0.0.0/16

# 私有子网 ID（至少 2 个，用于高可用）
PRIVATE_SUBNET_1_ID=subnet-xxxxxxxxxxxxxxxxx
PRIVATE_SUBNET_2_ID=subnet-yyyyyyyyyyyyyyyyy

# -----------------------------------------------------------------------------
# 可选配置
# -----------------------------------------------------------------------------

# VPC Endpoint 前缀（默认使用 ${COMPANY}-sm）
# ENDPOINT_PREFIX=acme-sm

# 安全组命名前缀（默认使用 ${COMPANY}-sagemaker）
# SG_PREFIX=acme-sagemaker

# =============================================================================
# 注意: 以下配置已移至共享文件 .env.shared:
#   - COMPANY, AWS_ACCOUNT_ID, AWS_REGION
# =============================================================================
