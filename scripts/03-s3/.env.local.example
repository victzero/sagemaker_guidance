# =============================================================================
# 03-s3 - S3 模块特有配置
# =============================================================================
# 共享配置请编辑: ../scripts/.env.shared
# 此文件包含 S3 模块特有的配置项（可选）
# =============================================================================

# -----------------------------------------------------------------------------
# S3 Bucket 配置（可选，有默认值）
# -----------------------------------------------------------------------------

# 加密类型: SSE-S3 (默认) 或 SSE-KMS
ENCRYPTION_TYPE=SSE-S3

# 如果使用 SSE-KMS，指定 KMS Key ARN（可选）
# KMS_KEY_ARN=arn:aws:kms:ap-southeast-1:123456789012:key/xxx

# 是否启用版本控制（默认 true）
ENABLE_VERSIONING=true

# -----------------------------------------------------------------------------
# Lifecycle 配置（可选）
# -----------------------------------------------------------------------------

# 不完整上传清理天数（默认 7）
ABORT_INCOMPLETE_DAYS=7

# 旧版本过期天数（默认 90）
NONCURRENT_EXPIRATION_DAYS=90

# 过期删除标记清理（默认 true）
DELETE_EXPIRED_MARKERS=true

# =============================================================================
# 注意: 以下配置已移至共享文件 .env.shared:
#   - COMPANY, AWS_ACCOUNT_ID, AWS_REGION
#   - TEAMS, *_PROJECTS (用于生成 Bucket 名称)
# =============================================================================
