# =============================================================================
# SageMaker Platform - Shared Environment Variables
# =============================================================================
# 所有脚本 (01-iam, 02-vpc, 03-s3) 共享的配置
#
# 使用方法:
#   1. 复制此文件: cp .env.shared.example .env.shared
#   2. 编辑 .env.shared 填入实际值
#   3. 各子目录的脚本会自动加载此文件
# =============================================================================

# -----------------------------------------------------------------------------
# AWS 基础配置
# -----------------------------------------------------------------------------

# 公司/组织前缀（用于资源命名，建议 3-8 字符小写）
COMPANY=acme

# AWS 账号 ID（12 位数字）
AWS_ACCOUNT_ID=123456789012

# AWS 区域
AWS_REGION=ap-southeast-1

# AWS Profile（可选，留空使用默认或 CloudShell 内置凭证）
# AWS_PROFILE=tokyo

# -----------------------------------------------------------------------------
# 团队配置
# -----------------------------------------------------------------------------

# 团队 ID 列表（空格分隔，小写简称）
TEAMS="rc algo"

# 团队全称（用于 IAM 资源命名，格式: TEAM_{ID}_FULLNAME）
TEAM_RC_FULLNAME=risk-control
TEAM_ALGO_FULLNAME=algorithm

# -----------------------------------------------------------------------------
# 项目配置
# -----------------------------------------------------------------------------

# 各团队的项目列表（格式: {TEAM_ID}_PROJECTS）
RC_PROJECTS="project-a project-b"
ALGO_PROJECTS="project-x project-y"

# -----------------------------------------------------------------------------
# 用户配置
# -----------------------------------------------------------------------------

# 平台管理员（可访问所有资源）
ADMIN_USERS="jason"

# 项目成员（格式: {TEAM_ID}_{PROJECT_NAME}_USERS）
# PROJECT_NAME: 连字符(-) 转为下划线(_)，全大写
RC_PROJECT_A_USERS="alice bob"
RC_PROJECT_B_USERS="carol"
ALGO_PROJECT_X_USERS="david"
ALGO_PROJECT_Y_USERS="eve"

# -----------------------------------------------------------------------------
# 通用设置
# -----------------------------------------------------------------------------

# 输出目录（相对于各脚本目录）
OUTPUT_DIR=./output

# =============================================================================
# 以上配置被以下脚本共享:
#   - 01-iam: 创建 IAM Policies, Groups, Users, Roles
#   - 02-vpc: 创建 Security Groups, VPC Endpoints
#   - 03-s3:  创建 S3 Buckets, Policies, Lifecycle Rules
# =============================================================================
